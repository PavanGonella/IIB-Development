

CREATE COMPUTE MODULE getGetDetails_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE var REFERENCE TO InputRoot.XMLNSC.Employee.Details.Id;
		SET OutputRoot.JSON.Data.Result.Data=PASSTHRU('SELECT * FROM EMPLOYEES_DATA WHERE EMPLOYEE_ID =?'VALUES(var));
		RETURN TRUE;
	END;

END MODULE;
