

CREATE COMPUTE MODULE CompleteParsing_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE root CHARACTER CAST(ASBITSTREAM(InputRoot) AS CHARACTER);
		CALL ParsingTypes(MessageFlowLabel,root);
		RETURN TRUE;
	END;

	
END MODULE;
CREATE PROCEDURE ParsingTypes(IN MessageFlowType CHARACTER,IN RootData CHARACTER) 
LANGUAGE DATABASE
EXTERNAL NAME "PARSINGTYPESPROC";