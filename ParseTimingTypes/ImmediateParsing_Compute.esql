

CREATE COMPUTE MODULE ImmediateParsing_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE root CHARACTER CAST(ASBITSTREAM(InputRoot) AS CHARACTER);
		CALL ParsingTypes(MessageFlowLabel,root);
		RETURN TRUE;
	END;

END MODULE;
