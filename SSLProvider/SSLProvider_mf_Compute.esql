

CREATE COMPUTE MODULE SSLProvider_mf_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE InputId REFERENCE TO InputRoot.XMLNSC.Customers.Id;
		SET OutputRoot.JSON.Data.Result.Values[] = PASSTHRU('SELECT * FROM CUSTOMERS WHERE ID=?'VALUES(InputId));
		RETURN TRUE;
	END;

END MODULE;
